% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/hx_select_input.R
\name{hx_select_input}
\alias{hx_select_input}
\title{Select input}
\usage{
hx_select_input(
  id,
  label = NULL,
  choices,
  selected = NULL,
  multiple = FALSE,
  name = id,
  class = NULL,
  get = NULL,
  post = NULL,
  target = NULL,
  swap = NULL,
  trigger = NULL,
  indicator = NULL,
  swap_oob = NULL,
  confirm = NULL,
  ...
)
}
\arguments{
\item{id}{Element id. Also used as \code{name} by default.}

\item{label}{Optional label text. When provided, the input is wrapped in a
\verb{<div>} with a \verb{<label>}.}

\item{choices}{Named or unnamed character vector of choices. If unnamed,
values are used as labels. If named, names are used as labels and values
as option values (same convention as Shiny).}

\item{selected}{Optional value(s) to pre-select.}

\item{multiple}{Logical. If \code{TRUE}, adds the \code{multiple} attribute to allow
multi-selection.}

\item{name}{Form field name. Defaults to \code{id}.}

\item{class}{Optional CSS class(es) for the \verb{<select>} element.}

\item{get}{URL for \code{hx-get}.}

\item{post}{URL for \code{hx-post}.}

\item{target}{CSS selector for \code{hx-target}.}

\item{swap}{Swap strategy for \code{hx-swap}.}

\item{trigger}{Trigger specification for \code{hx-trigger}.}

\item{indicator}{CSS selector for \code{hx-indicator}.}

\item{swap_oob}{Out-of-band swap targets for \code{hx-swap-oob}.}

\item{confirm}{Confirmation message for \code{hx-confirm}.}

\item{...}{Additional HTML attributes passed to the \verb{<select>} element.}
}
\value{
An \link[htmltools:builder]{htmltools::tags} object.
}
\description{
Creates a \verb{<select>} element with optional htmx attributes.
When \code{label} is provided, the input is wrapped in a \verb{<div>} containing a
\verb{<label>} element linked via the \code{for} attribute.
}
\examples{
# Simple select without label
hx_select_input("cut", choices = c("Fair", "Good", "Ideal"))

# Select with label and named choices
hx_select_input(
  "cut",
  label = "Filter by cut:",
  choices = c("All" = "all", "Fair", "Good", "Ideal"),
  selected = "all"
)

# Select with htmx attributes
hx_select_input(
  "cut",
  label = "Filter by cut:",
  choices = c("All" = "all", "Fair", "Good"),
  get = "/rows",
  trigger = "change",
  target = "#tbody"
)

}
