% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/hx_table.R
\name{hx_table}
\alias{hx_table}
\title{Table with htmx-powered tbody}
\usage{
hx_table(
  columns,
  data = NULL,
  tbody_id = NULL,
  col_labels = NULL,
  col_classes = NULL,
  class = NULL,
  thead_class = NULL,
  get = NULL,
  post = NULL,
  put = NULL,
  patch = NULL,
  delete = NULL,
  target = NULL,
  swap = NULL,
  trigger = NULL,
  indicator = NULL,
  swap_oob = NULL,
  confirm = NULL,
  params = NULL,
  include = NULL,
  push_url = NULL,
  select = NULL,
  vals = NULL,
  encoding = NULL,
  headers = NULL,
  ...
)
}
\arguments{
\item{columns}{Character vector of column names to display. Defines the
\verb{<thead>} structure (required).}

\item{data}{Optional data frame. If provided, rows are rendered in the
\verb{<tbody>} via \code{\link[=hx_table_rows]{hx_table_rows()}}. If \code{NULL}, the \verb{<tbody>} is empty.}

\item{tbody_id}{\code{id} attribute applied to the \verb{<tbody>} — not the \verb{<table>}.}

\item{col_labels}{Labels for the \verb{<thead>}. If \code{NULL}, column names are used
as-is. Can be a named vector (\code{c(price = "Price ($)")}) to override
specific columns, or an unnamed positional vector to replace all labels.}

\item{col_classes}{Named list of CSS classes for \verb{<td>} cells, passed to
\code{\link[=hx_table_rows]{hx_table_rows()}} when \code{data} is provided.}

\item{class}{CSS class(es) for the \verb{<table>} element.}

\item{thead_class}{CSS class(es) for the \verb{<thead>} element.}

\item{get}{URL for \code{hx-get} (applied to \verb{<tbody>}).}

\item{post}{URL for \code{hx-post} (applied to \verb{<tbody>}).}

\item{put}{URL for \code{hx-put} (applied to \verb{<tbody>}).}

\item{patch}{URL for \code{hx-patch} (applied to \verb{<tbody>}).}

\item{delete}{URL for \code{hx-delete} (applied to \verb{<tbody>}). Note: parameters
are sent in the URL query string — read them via \code{request$query}.}

\item{target}{CSS selector for \code{hx-target} (applied to \verb{<tbody>}).}

\item{swap}{Swap strategy for \code{hx-swap} (applied to \verb{<tbody>}).}

\item{trigger}{Trigger specification for \code{hx-trigger} (applied to \verb{<tbody>}).}

\item{indicator}{CSS selector for \code{hx-indicator} (applied to \verb{<tbody>}).}

\item{swap_oob}{Out-of-band swap targets for \code{hx-swap-oob} (applied to \verb{<tbody>}).}

\item{confirm}{Confirmation message for \code{hx-confirm} (applied to \verb{<tbody>}).}

\item{params}{Parameters to submit for \code{hx-params}. Use \code{"*"} to include
all parameters (equivalent to omitting this argument), \code{"none"} to send
none, or a comma-separated list of names (e.g. \code{"id, name"}). Prefix with
\code{not} to exclude specific parameters (e.g. \code{"not id, name"}).}

\item{include}{CSS selector for \code{hx-include}. Additional elements whose
values are included in the request. htmx relative selectors are valid:
\code{"closest form"}, \code{"find input"}, \code{"next .sibling"}. Note: \code{params = "none"} does \strong{not} suppress values sourced via \code{include}.}

\item{push_url}{Push a URL into the browser history for \code{hx-push-url}
(applied to \verb{<tbody>}). Use \code{"true"}, \code{"false"}, or a custom URL.}

\item{select}{CSS selector for \code{hx-select} (applied to \verb{<tbody>}). Extracts
a specific element from the server response before swapping.}

\item{vals}{JSON string of extra values for \code{hx-vals} (applied to
\verb{<tbody>}). Values are passed as-is (e.g. \code{'{"id": 42}'}).}

\item{encoding}{Encoding type for \code{hx-encoding} (applied to \verb{<tbody>}).
Use \code{"multipart/form-data"} to enable file uploads.}

\item{headers}{JSON string of request headers for \code{hx-headers} (applied to
\verb{<tbody>}). Values are passed as-is.}

\item{...}{Additional HTML attributes passed to the \verb{<table>} element.}
}
\value{
An \link[htmltools:builder]{htmltools::tags} object (\verb{<table>}).
}
\description{
Builds a complete \verb{<table>} element with a \verb{<thead>} and a \verb{<tbody>}.
htmx attributes are applied to the \verb{<tbody>}, making it the swap target.
When \code{data} is \code{NULL} (the default), the \verb{<tbody>} is empty and its content
is loaded lazily via htmx (e.g. \code{trigger = "load"}).
}
\examples{
# Lazy-load table (empty tbody, content loaded on trigger)
hx_table(
  columns = c("cut", "color", "price"),
  col_labels = c("Cut", "Color", "Price"),
  tbody_id = "tbody",
  get = "/rows",
  trigger = "load",
  swap = "innerHTML"
)

# Table with data pre-rendered
df <- data.frame(cut = c("Fair", "Good"), price = c(326L, 400L))
hx_table(columns = c("cut", "price"), data = df)

}
