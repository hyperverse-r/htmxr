% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/hx_trigger.R
\name{hx_trigger}
\alias{hx_trigger}
\alias{hx_trigger_after_swap}
\alias{hx_trigger_after_settle}
\title{Trigger client-side htmx events via response headers}
\usage{
hx_trigger(response, event)

hx_trigger_after_swap(response, event)

hx_trigger_after_settle(response, event)
}
\arguments{
\item{response}{A plumber2 response object. The parameter \strong{must} be named
\code{response} in route handlers — it is the only name plumber2 recognises for
injecting the response object.}

\item{event}{One of:
\itemize{
\item A \strong{character string} — fires a single event: \code{"myEvent"}.
\item A \strong{character vector} — fires multiple events: \code{c("event1", "event2")}.
\item A \strong{named list} — fires events with detail payloads:
\code{list(showMessage = list(level = "info"), confetti = NULL)}.
Each name is an event; each value is its detail (use \code{NULL} for no detail).
}}
}
\value{
The response object \code{response}, invisibly.
}
\description{
Adds an \code{HX-Trigger}, \code{HX-Trigger-After-Swap}, or \code{HX-Trigger-After-Settle}
header to a plumber2 response, causing htmx to fire one or more client-side
events after the response is received.
}
\details{
\subsection{Timing variants}{
\itemize{
\item \code{hx_trigger()} — fires immediately when the response is received
(\code{HX-Trigger}).
\item \code{hx_trigger_after_swap()} — fires after htmx swaps the new content into
the DOM (\code{HX-Trigger-After-Swap}). Use this when the event handler needs
to interact with the freshly-swapped elements.
\item \code{hx_trigger_after_settle()} — fires after htmx settles (CSS transitions
complete) (\code{HX-Trigger-After-Settle}).
}
}

\subsection{Detail serialisation}{

When \code{event} is a named list, values are serialised to JSON using a minimal
built-in serialiser that supports \code{NULL}, logicals, numbers, strings, and
named lists of the above. No external dependency required.
}
}
\examples{
\dontrun{
#* @post /submit
function(response) {
  # Simple event
  hx_trigger(response, "formSubmitted")

  # Multiple events
  hx_trigger(response, c("formSubmitted", "refresh"))

  # Event with detail payload
  hx_trigger(response, list(showMessage = list(level = "info", text = "Saved!")))

  list(status = "ok")
}
}

}
